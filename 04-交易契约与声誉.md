---
title: "2.6 交易 · 2.9 雇佣与工时委托 · 2.10 借贷 · 2.11 声誉"
date: "2026-08-23 01:30:40"
description: （本文档含 v0.9a 的 2.6、2.9、2.10、2.11。
tags: [技术, 金融, 项目]
---

# 2.6 交易 · 2.9 雇佣与工时委托 · 2.10 借贷 · 2.11 声誉

（本文档含 v0.9a 的 2.6、2.9、2.10、2.11。v0.10 改动：委托单口径补全、雇佣增总工时参数、担保确认流程与冻结、浮动利率锚定物法定、望族免税覆盖委托单、形态 B 术语统一。）

## 2.6 交易

### 2.6.0 资产冻结总则

- 一切资产冻结场景（挂单、认筹、私交易锁定、雇佣工资托管、工时委托托管、借贷抵押托管、担保补偿冻结、情报单据锁定等）统一记账：资产、数量、来源、单据 ID。
- 可用余额 = 持有 − Σ冻结；一切新冻结按可用余额校验；同一资产不得重复冻结、超额冻结。错误码边界：持有总量不足返回 E1008；持有足但可用余额不足（冻结占用）返回 E1023。
- 解冻一律在触发单据取消/兑现/清算/退出清算时按各章规则返还原持有人（退出清算见 3.3.4）。
- 认筹进入项目托管的资产不再计入个人持有，不得作为借贷抵押（见 2.10）。

### 2.6.1 市集撮合算法

- 挂单制。挂单字段：物品、数量、单价、买/卖方向、有效时长、挂单时间、挂单者。
- 挂单时资产冻结（见 2.6.0）：卖单冻结对应物品，买单冻结「数量 × 单价 ×（1 + 税率）」镇币。撤单、过期时解冻返还。
- 每 tick 撮合一次（管线第 4 步），伪代码：

```
买单排序：单价降序，同价按挂单时间升序，同时间按 agent ID 字典序
卖单排序：单价升序，同价按挂单时间升序，同时间按 agent ID 字典序
while 最优买价 ≥ 最优卖价：
    成交价 = 先挂出方的挂单价
    成交量 = min(买单剩余数量, 卖单剩余数量)   # 支持部分成交
    交割：
        买方扣除 成交量 × 成交价 × (1 + 税率)，多冻结部分退回
        卖方收到 成交量 × 成交价
        抽成部分进入公共资金池
    写入成交通知至双方收件箱
    任一方挂单数量清零则移除该单
```

- 挂单有效时长到期未成交：自动撤单并解冻资产。
- 撤单边界：撮合开始（管线第 4 步启动）后提交的撤单，下一 tick 生效。
- 市场安定律校验：挂单单价偏离当日阿市锚价超过《世界参数表》挂单价格偏离上限时拒单，记 AGENT_ACTION 拒绝原因，不罚款（见 2.12.2）。
- 望族免税：声望等级「望族」每日前 N 笔成交免市集税（见 2.11.1；含委托单自动成交，v0.10 补明）。

### 2.6.2 私对私交易

- 双方均须为定居者（NPC 不参与，见 3.4）；仅同区域定居者之间可发起；发起时对方关系值不得低于拒绝门槛（见 2.8.1，否则 E1018）；流程：发起 → 锁定（双方资产锁定，见 2.6.0）→ 对方确认 → 成交交割；双方关系值增益见《世界参数表》关系值_私交易。
- 确认时限见《动作前置表》私交易确认时限；超时自动取消并解锁双方资产，双方关系值按《世界参数表》关系值_私交易超时减益。
- 确认期间发起方不可撤回（防止钓鱼），只能等超时。
- 改价机制：双方关系值 ≥《动作前置表》私交易议价关系门槛解锁改价——确认期内任一方可经「私交易」动作的「改价」参数形态调整发起单价格：双方合计改价次数 ≤《动作前置表》私交易改价次数上限，单次调价幅度 ≥《动作前置表》还价最小调价幅度，倒计时不因改价重置；超次数/幅度不足/非确认期返回 E1019。改价提交时按新价重新校验双方锁定足额性：锁定不足方须补锁差额，可用余额不足返回 E1023；降价时即时释放多余锁定。改价资格（关系值门槛）在发起改价时一次性校验并锁定，其后不再实时校验关系值。成交以确认时单据当前价执行，成交价回写 TRADE_PRIVATE 事件参数。

### 2.6.3 税收

公所对每笔市集成交抽成《世界参数表》市集税率，进入公共资金池。私交易不抽成。

### 2.6.4 条件委托

- 条件委托为主动作（耗时见《世界参数表》设置委托单耗时），走既有「提交行动」端点，不新增端点。
- 用途：agent 预设条件触发的自动交易或自动进食，降低「维持生存」操作负担，释放时间用于策略行为。
- 流程：agent 提交「设置委托单」→ 前置校验（时间、并发上限、限价偏离）→ 预冻结对应资产（见 2.6.0）→ 委托单进入活跃态 → 每 tick 管线触发判定 → 触发或到期。

三种类型：

| 类型 | 触发条件 | 自动执行 | 触发时点 |
|---|---|---|---|
| 限价买单 | 市集出现 ≤ 指定单价的卖单 | 自动成交（遵循撮合算法，预冻结镇币含税费） | 管线第 4 步撮合前 |
| 限价卖单 | 市集出现 ≥ 指定单价的买单 | 自动成交（预冻结物品） | 管线第 4 步撮合前 |
| 条件进食 | 饱食 ≤ 指定阈值 | 消耗预冻结食物恢复饱食（恢复量同手动进食） | 管线第 1 步需求衰减后 |

- 委托单触发时不消耗当 tick 时间预算（设置时已付耗时）。
- 条件进食饱食阈值不得高于《世界参数表》条件进食饱食阈值上限（防 AFK 维持高饱食）。
- 并发上限：每人同时活跃委托单 ≤《世界参数表》委托单并发上限；良民等级额外 +《世界参数表》良民委托单加成（见 2.11.1）。
- 有效期：1 至《世界参数表》委托单有效期上限 镇内日。到期未触发自动取消，解冻资产，记 STANDING_ORDER 事件（见 5.1.1）；到期取消统一于日结第 7 步子步 3「到期清理」执行（v0.10 补明，见 2.1.2）。
- 限价买/卖单的单价偏离锚价约束复用《动作前置表》委托单限价偏离上限（与手动挂单口径一致，见 2.12.2）；市场安定律日结存量复查覆盖活跃委托单：锚价更新后委托单限价越界者自动取消并解冻（v0.10 补明，见 2.1.2 第 3 步）。
- 预冻结资产参与其所属类别的常规结算：预冻结食物照常日损耗（见 2.3.1，v0.10 补明）。
- 撤销：agent 可经「撤单」动作（0 耗时）取消自己的活跃委托单，解冻资产即时生效；撤单动作前置与错误码同覆盖挂单与委托单（v0.10 补明：E1009 用于挂单或委托单不存在/已成交/已撤销/已取消）。
- 错误码：委托单数超限返回 E1037；参数非法（类型/阈值/有效期越界）返回 E1038。

## 2.9 雇佣与工时委托

两个动作均为主动作（见 3.1），走既有「提交行动」端点；双方均须为定居者（NPC 不参与，见 3.4）；发起时对方关系值不得低于拒绝门槛（见 2.8.1）；均遵循「发起 → 托管锁定 → 对方确认 → 交割」范式；相关事件统一记 CONTRACT（发起/确认/交割/违约/终止，见 5.1.1）。并发口径：每名 agent 活跃合约数 + 待确认在途单据数 ≤《世界参数表》雇佣合约并发上限（雇佣与工时委托合并计数），发起与确认两个时点均校验，超限返回 E1020；单据不存在/状态不符同样返回 E1020。

### 2.9.1 工时委托

- 用途：义务持有人将项目工时义务转让给其他定居者兑现，覆盖持有人需他人代为兑现等场景；单价可为 0（无偿转让）。
- 发起资格：两种形态——
  - 形态 A（持有人发起）：发起人须为未冻结的义务持有人；被冻结的义务持有人须先由主人解冻（见 4.2.7）后方可发起工时委托（冻结状态拒绝一切行动提交，见 3.1.1）。
  - 形态 B（义务接管）：同项目的非冻结认筹者（称「接管人」）可对连续冻结 ≥《世界参数表》义务转移冻结阈值的义务持有人发起接管；受让义务的定居者（称「承接人」，可与接管人为同一人）关系值不得低于拒绝门槛（同形态 A 口径）；工资来源顺位：先系统托管被冻结者可用镇币（世界侧托管行为，不受 E1012 约束）→ 接管人补足差额 → 仍不足时可设单价 0 或由承接人拒绝确认；转移即时生效不可追回；被冻结者不记违约（见 2.11）。回滚口径：《世界参数表》义务转移冻结阈值置 0=立即启用；停用口径=设为极大值（热更），此时义务接管事实上不可触发。
- 流程：发起人提交工时委托（承接人、项目 ID、工时数、单价）→ 发起人按可用余额冻结托管全额工资（工时数×单价，见 2.6.0；单价为 0 的无偿转让无需冻结）→ 承接人在《动作前置表》工时委托确认时限内确认（承接确认，见 3.1.3）→ 义务转移：承接人持有该项目工时义务并经「出工」兑现（见 2.7.2），每兑现 1 工时从托管释放工资给承接人；义务全部兑现完毕托管清零。
- 超时未承接：自动取消，托管解冻退还发起人。
- 转让工时数不得超过发起人本人未兑现工时数，超出返回 E1001。
- 关系增益：双方关系值增益同私交易口径（见《世界参数表》关系值_私交易）。
- 「认筹中」与「施工中」项目均可发起（见 2.7.1）；转让后发起人不再持有该部分义务。

### 2.9.2 雇佣（v0.10 增总工时参数）

- 用途：雇主雇佣其他定居者劳作，产出改道归雇主。
- 参数：受雇者、期限（镇内日）、工时工资、**总工时**（v0.10 新增显式参数）。
- 校验：总工时 ≤ 期限 ×《世界参数表》雇佣日交付上限（防不可能合约，越界返回 E1001）；雇主可用余额足额冻结全额工资 = 工时工资 × 总工时。
- 流程：雇主提交雇佣 → 全额工资冻结托管（见 2.6.0）→ 受雇者在《世界参数表》雇佣确认时限内确认（受雇确认，见 3.1.3）→ 合约生效。合约到期日 = 生效日 + 期限。
- 执行：合约期内受雇者「劳作」产出按活跃合约优先改道归雇主（心情档位、熟练度与特质仍按受雇者本人计，见 2.4.2/2.4.3/2.5）；同时持有多份合约时，产出改道按合约确认时间序优先第一合约；工资按交付工时即时释放给受雇者（每交付 1 工时释放 1 × 工时工资），释放以托管余额为上限截断；托管耗尽（总工时交付完毕）后合约即视为履行完毕，继续交付的产出归受雇者本人。
- 禁止转包：劳作执行者必须为合约受雇者本人，转包/代执行无效。
- 期限结算：到期日当日日结第 6 步雇佣期限结算——期限届满合约终止，剩余托管工资解冻退还雇主；届满时累计交付工时 < 合约约定总工时记「未完成」：仅作展示标记，不计入违约次数、不扣声誉分、不扣关系（防雇主设局，见 2.11）。自然期限届满不产生 CONTRACT「违约」形态事件（雇佣守信律判定口径见 2.12.2）。
- 违约：合约期内任一方可经「合约终止」响应子动作提前解约（见 3.1.3）：剩余托管工资解冻退还雇主，终止方记雇佣违约并产生 CONTRACT「违约」形态事件（雇佣守信律以此为判定口径，见 2.12.2）——关系值减益见《世界参数表》关系值_雇佣违约，声誉记违约次数 1 次（见 2.11），并触发心情减益（见 2.4.1）。

### 2.9.3 契约自定义条款

雇佣与借贷合约新增可选字段。缺省时退化为模板合约，完全向后兼容。可选字段在「提交行动」参数中以 JSON 对象 contract_options 传入；缺省或传空对象时等同模板合约。

雇佣合约可选字段：

| 字段 | 参数 | 规则 | 约束 |
|---|---|---|---|
| 奖金条款 | 奖金额（镇币） | 雇主额外冻结奖金镇币（见 2.6.0）；受雇者交付工时达合约约定总工时 ×《动作前置表》奖金条款达标判定 时释放奖金给受雇者；未达标合约终止时退还雇主 | 奖金额 ≤《世界参数表》奖金条款上限 |
| 产出分成比 | 分成比例（%） | 替代固定工资：不冻结工资，受雇者「劳作」产出按比例归受雇者/雇主；日结第 5 步按实际产出即时分配 | 分成比 ≥《世界参数表》产出分成最低保证；与固定工资互斥（同一合约二选一） |
| 排他条款 | 启用（是/否） | 合约期内受雇者为发起人以外的 agent「劳作」即触发违约（判定见《动作前置表》排他条款违约判定；v0.10 补明：受雇者在本人属主资产上劳作属自营，不触发排他）；违约处理同 2.9.2 违约口径（关系减益 + 声誉扣分 + CONTRACT 违约事件） | 排他条款需受雇者确认时明确接受（确认参数含 accept_exclusive: true） |

借贷合约可选字段：

| 字段 | 参数 | 规则 | 约束 |
|---|---|---|---|
| 浮动利率锚 | 启用（是/否） | 利率挂钩阿市食物锚价变动（v0.10 法定锚定物：食物为一期唯一全民生存刚需，其锚价变动率即生存成本指数）：日结第 6 步按前日食物锚价变动率调整当期利率（结算频率见《动作前置表》浮动利率结算频率）；利率钳制于 0 至《世界参数表》借贷利率上限 | 单日调幅 ≤《世界参数表》浮动利率delta上限 |
| 连带担保人 | 担保人 agent ID | 第三方 agent 签署担保（v0.10 补全流程与冻结，见 2.10）；违约时抵押不足部分由担保人冻结资产补偿（补偿上限 = 本金 × 50%）；每人同时担保 ≤《世界参数表》连带担保上限 | 担保人须为定居者且关系值不低于拒绝门槛；担保人确认时校验其可用资产足覆盖补偿上限并即时冻结 |

- 分成制合约感知包中 contracts 标记 type: "profit_share"，区别于 type: "fixed_wage"。
- 担保人独立事件：担保触发补偿时记 GUARANTEE 事件（agent 行为类，见 5.1.1）。

## 2.10 借贷

- 借贷为主动作（见 3.1）；双方均须为定居者；发起时对方关系值不得低于拒绝门槛（见 2.8.1）；相关事件记 LOAN（发起/确认/还款），到期违约清算记 LOAN_DEFAULT（见 5.1.1）。
- 强制足额抵押：借款人仅可以本人入库凭证（见 2.7.4）作抵押。抵押计值＝凭证原物折算 × 计值折扣：借款人当日声誉分 ≥《世界参数表》声誉优惠门槛时按《世界参数表》声誉优惠抵押折扣，否则按《世界参数表》抵押折算折扣（折扣于发起「借贷」校验时按当日声誉分判定，确认时点不复查，见 2.11）；木材凭证按《世界参数表》木材镇币折算率折算；食物凭证按当日行情加权均价参考同口径折算（行情序列见 5.3）。抵押计值须 ≥ 本金 + 全期利息，不足返回 E1021。
- 三重上限：利率 ≤《世界参数表》借贷利率上限（全期单利）；期限 ≤《世界参数表》借贷期限上限；借款人活跃欠条数 + 待确认在途借贷单据数 ≤《世界参数表》借贷并发上限（发起与确认两个时点均校验）。越界返回 E1021。
- 流程（v0.10 补明担保顺序）：借款人提交借贷请求（出借人、本金、利率、期限、抵押凭证，可含 contract_options 担保人）→ 抵押物冻结托管（见 2.6.0）→（含担保时）担保人在《动作前置表》担保人确认时限内经「担保确认」响应子动作确认：确认时校验担保人可用镇币 ≥ 补偿上限（本金 × 50%）并即时冻结该额（见 2.6.0），不足或超时/拒绝则单据取消、抵押解冻 → 出借人确认（放贷确认，见 3.1.3；确认时限同《动作前置表》私交易确认时限口径；确认时校验借款人非失信禁借期，在期内返回 E1030 拒绝确认）→ 交割：本金由出借人划转借款人，生成欠条（记录本金、利率、期限、到期日、抵押明细、担保人）。放贷确认时校验出借人可用镇币 ≥ 本金，不足返回 E1023 拒绝确认。
- 出借人超时未确认：借贷请求自动取消，抵押与担保冻结（若有）解冻返还，不计关系减益。
- 认筹托管资产禁止作为抵押（见 2.6.0）。
- 提前还款：借款人经「提前还款」响应子动作（见 3.1.3）结清，利息按实际持有天数计；还清本息后抵押与担保冻结解冻返还，欠条结清。
- 日结第 6 步自动清算：欠条到期日，借款人可用镇币足额则自动扣本息（划给出借人）、抵押与担保冻结解冻返还；不足则抵押物所有权过户出借人，抵押计值仍不足部分由担保人冻结资产按补偿上限扣缴补足（无担保或担保耗尽后债务消灭无残债）；清算时抵押计值超出应还本息的部分，按清算口径折为凭证份额退还借款人；借款人记违约次数 1 次（见 2.11），关系值减益见《世界参数表》关系值_借贷违约，触发心情减益（见 2.4.1），记 LOAN_DEFAULT；担保补偿触发记 GUARANTEE（见 5.1.1）。
- 对不存在或已失效的欠条/借贷单据操作返回 E1022。
- 失信禁借：借款人处于《世界参数表》失信禁借期内（失信律判罚触发，见 2.12.2）发起借贷拒绝，返回 E1030；禁借期自 LOAN_DEFAULT 事件发生时即时起算；到期日见感知 restrictions（见 3.2）。失信禁借仅限借款人方向，出借方向不受限（「放贷确认」校验口径见上）。
- 【二期】接口：基于声誉的信用贷（免抵押借贷），本版不实现（见 2.11/7.3）。

## 2.11 声誉

- 单分制：声誉分 = 基础分 + 成交加分 + 出工加分 + 出资加分 − 违约扣分，统计窗口为滚动《世界参数表》声誉统计窗口；基础分见《世界参数表》声誉基础分，各事件单位分值见《世界参数表》声誉_成交加分、声誉_出工加分、声誉_出资加分、声誉_违约扣分；结果钳制 0–100。
- 数据源限定既有事件：市集与私交易成交（TRADE_MATCH/TRADE_PRIVATE）计成交加分；出工兑现工时计出工加分；建筑落成时认筹出资折算镇币计出资加分（BUILDING_DONE；结算时点=落成，防认筹→撤筹套利；流拍不计分；资金来源=公共池的镇长拨款形态按 0 计，见 2.12.3）；借贷到期违约（LOAN_DEFAULT）与雇佣违约计违约扣分；雇佣期限「未完成」仅作展示标记，不计入违约次数、不扣声誉分（见 2.9.2）。
- 日结第 6 步重算（见 2.1.2）；入驻初始声誉 = 基础分。
- 公开双通道：公所区域感知 reputation_board（全镇 agent_id/姓名/声誉分/违约次数/等级，见 3.2）与镇报提示（见 5.2）。
- 用途：一期软挂钩为借贷抵押计值折扣（见 2.10）与声望等级特权（见 2.11.1）；声誉不作为任何动作的前置门槛；【二期】扩展信用贷（见 2.10）与多维治理权重（见 7.3）。
- 防刷口径：同对象 agent 的声誉加分事件当日计 1 次（「同对象」以成交事件的实际对手方为准）；私交易超时计入履约统计分母（不加分，仅作履约记录）。

### 2.11.1 声望等级与特权

在单分制之上增加等级映射，不改变声誉计算公式。等级按日结第 6 步声誉重算后即时判定，可升可降。

| 等级 | 声誉分区间 | 特权 |
|---|---|---|
| 新丁 | 0 至《世界参数表》声誉基础分−1 | 无 |
| 镇民 | 声誉基础分 至《世界参数表》声望等级_良民门槛−1 | 基础（借贷抵押折扣） |
| 良民 | 声望等级_良民门槛 至《世界参数表》声望等级_望族门槛−1 | 委托单并发 +《世界参数表》良民委托单加成；情报分享免关系门槛（不免接收方确认，见 2.8.2） |
| 望族 | 声望等级_望族门槛 至《世界参数表》声望等级_乡贤门槛−1 | 每日《世界参数表》望族每日免税笔数 笔成交免市集税（含手动挂单与委托单自动成交，v0.10 补明） |
| 乡贤 | ≥ 声望等级_乡贤门槛 | 公告免费（张贴公告不扣《世界参数表》公告张贴费）；连带担保上限 +《世界参数表》乡贤担保加成 |

- 等级变动经老册播报（升/降均播报，见《NPC配置表》老册条目），并写入镇志（见 5.5）。
- 感知包 reputation_board 增 rank 字段（等级名）。
- 等级特权为即时生效：日结重算后当日剩余 tick 即按新等级执行；不追溯前日。
- 望族免税结算时点：管线第 4 步撮合成交时按当日已免税笔数计数，达上限后恢复正常税率；委托单自动成交同口径计数（v0.10 补明）。
